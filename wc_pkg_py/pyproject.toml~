[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wc_go"
version = "0.0.1"
authors = [
  { name="Thomas Prior" },
]
description = "An implementation of wc in Go"
requires-python = ">=3.0"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: *_x86",
]

